server:
  port: 80
cors:
  originPatterns: http://localhost:8080,https://asdsd.com
file:
  upload-dir: /uploadfile  
security:
  jwt:
      token:
        secret-key: 53ccr37
        expire-length: 3600000  
spring:
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/rest_spring?useTimeZone=true&serverTimeZone=UTC
      username: root
      password: admin123
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    # properties:
     #  hibernate: 
      # dialect: org.hibernate.dialect.MySQLDialect               
    show-sql: false    
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2KB
      max-file-size: 20MB
      max-request-size: 215MB
springdoc:
  pathsToMatch: /api/**/
  swagger-ui:
    use-root-path: true     
    
      
      
   
      
      
      
      
      
   
      
      
      
